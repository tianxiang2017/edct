spring:
  application:
    name: registry
  cloud:
    config:
      uri: http://config.ztxlearn.xyz:8888
      fail-fast: true
      username: user
      password: ${CONFIG_SERVICE_PASSWORD:password}
